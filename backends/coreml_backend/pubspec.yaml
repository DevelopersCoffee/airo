name: coreml_backend
description: Component for coreml_backend
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_backend:
    path: ../../packages/platform_backend

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
