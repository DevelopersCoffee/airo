library mediapipe_backend;
import 'package:platform_backend/platform_backend.dart';

class MediapipeBackendProvider implements BackendProvider {
  @override
  String get name => 'mediapipe_backend';
}
