name: Plugin Size Gate

on:
  workflow_dispatch:

jobs:
  release-only-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Explain release-only check policy
        run: |
          echo "Automated PR size checks are disabled."
          echo "Cut a v* release tag to run release validation checks."
