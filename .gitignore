# Secrets and confidential files
*.secret
*.key
*.pem
*.p12
*.jks
*.keystore
app/android/key.properties
app/android/*.jks
app/android/*.keystore
google-services.json
firebase-adminsdk*.json
GoogleService-Info.plist

# E2E Test Authentication Sessions (sensitive - contains OAuth tokens)
e2e/googleAuth.json
e2e/*Auth.json
e2e/.auth/
playwright/.auth/

# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.g.dart
*.freezed.dart
# Exception: vendored third-party plugins ship their pigeon output upstream.
# Ignoring it here left Android builds unable to compile from a clean checkout.
# Regenerate from the vendored package with its pinned Pigeon version. Ensure
# Xcode's `swift-format` and `clang-format` are available on PATH so Dart and
# native host outputs are produced as one reviewed change.
!packages/platform_player/third_party/**/lib/src/*.g.dart

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment files
.env
.env.local
.env.*.local

# Dependencies
node_modules/
vendor/

# Logs
*.log
logs/
artifacts/agent-skills-journey/
artifacts/airo-tv-browser-viewports/
artifacts/store-listing/
.gstack/
$CODEX_HOME/
app/pubspec.yaml.backup
.superpowers/

# Database
*.db
*.sqlite
*.sqlite3

# Temporary files
*.tmp
*.temp
.cache/

# Build artifacts
release-artifacts/
*.apk
*.aab
*.ipa
**/.gradle/

# Patrol generated test bundles
app/patrol_test/test_bundle.dart
app/patrol_test/Users/

# ===========================================
# Python (iptv-data/)
# ===========================================

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environments
venv/
ENV/
env/
.venv/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Type checking / Linting
.mypy_cache/
.ruff_cache/

# IPTV Pipeline Output (published to GitHub Gist, not committed to repo)
iptv-data/output/current/
iptv-data/output/previous/
iptv-data/output/archive/
iptv-data/output/reports/

# Keep output directory structure with .gitkeep files
!iptv-data/output/.gitkeep
!iptv-data/output/current/.gitkeep

# Rust build artifacts
rust/target/

.worktrees/
# Agent worktrees and scratch tooling (Claude Code, Codex, and friends)
.claude/worktrees/
.codex/
# Generated by scripts/check-build-profiles.py
build-profile-report.md
# Kotlin compiler daemon session files
**/.kotlin/

# Agent skills (installed globally / by plugins — not project source)
.claude/skills/
.claude/plugins/
.agents/skills/
.augment/skills/
skills-lock.json

# Exception: bundled skills that ship with the repo (installed via scripts/skills.sh)
!.claude/skills/android-tv-design/
!.claude/skills/android-tv-design/**

# IDE-generated workspace descriptors
*.iml

# Release artifacts staging directory
artifacts/release/

# Airo Mind model weights — 74 MB+, fetched by the Model Manager, never committed
rust/airo_mind_runtime/models/*.bin
rust/airo_mind_runtime/models/*.gguf

# `airo_mind_whisper`'s offline integration tests (tests/speech_offline.rs)
# look for a real GGML model here and skip gracefully when it is absent — a
# developer machine's local copy for running those tests, never source.
rust/airo_mind_whisper/models/*.bin

# Generated by the size-gate and model-artifact workflows. They are uploaded as
# build artifacts and posted as PR comments; a copy left in the working tree
# after a local run is output, not source.
module-size-report.md
model-artifact-report.md
