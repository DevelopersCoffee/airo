<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Airo App.

    Play Store V2 uses a bring-your-own-content media-player model. The app
    does not include first-party IPTV provider allowlists or broad cleartext
    exceptions.
-->
<network-security-config>
    <!-- Base configuration: HTTPS required by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug configuration for development builds -->
    <!-- Uncomment for debugging network issues with Charles/Fiddler proxy -->
    <!--
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    -->
</network-security-config>
