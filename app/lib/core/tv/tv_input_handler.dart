// TV input key handling — re-exported from core_ui, which owns the
// canonical implementation shared across the app and feature_iptv.
export 'package:core_ui/core_ui.dart'
    show
        TvInputCallback,
        TvInputHandler,
        TvInputKey,
        TvInputKeyExtension,
        TvInputResult;
