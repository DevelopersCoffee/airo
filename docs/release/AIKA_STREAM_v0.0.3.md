# Aika Stream v0.0.3

Aika Stream v0.0.3 is the Play drop for country programme guides plus two-pane
MultiView that snaps 5/50/95, keeps a short premium handle, and mixes the two
streams with the bar.

## Highlights

- Picking a country in Settings/browse loads that country's programme guide
  instead of a world-size dump. A pasted XMLTV URL still wins.
- Two-pane live split snaps to **5 / 50 / 95**, with an 80dp min pane so a
  phone never gets an 18px sliver.
- Dragging the handle crossfades the two videos with equal-power gains.
  System volume does not change.
- Compact (either axis under 600dp) uses a **48dp** full-seam hit; TV and
  tablet landscape use **24dp**. Paint is a 2dp hairline plus an 8×28
  stadium in the middle third.
- Resume the last live channel on launch (already on Internal 22).
- Play `versionName` **0.0.3**, `versionCode` **24**. Do not reuse 23.

## New Features

### IPTV

- Country picker downloads one country XMLTV gzip via `airo_epg` 1.2.0.
- World-file URLs and oversized gzips fail before inflate.
- Two-pane MultiView mix follows pane order, not the featured tile.
- Tile volume sliders stay in triple/quad; two-pane hides them.

### Android TV

- D-pad focuses the handle. Left/Right (or Up/Down if stacked) steps
  5 / 50 / 95 with no wrap at the extreme.
- Select/OK on the handle is still a no-op.

### Playback

- Leaving two-pane restores exclusive featured audio.
- Cast mute-all stays silent in two-pane instead of remixing.

### User Interface

- Phone, tablet, and Android TV / D-pad remain one listing
  (`leanback` not required).

## Improvements

- Magnetic stops replace the 30/70 clamp from Internal 22.
- Handle paint is a short grip instead of a full-height slab.

## Bug Fixes

- Two-pane `muteAll()` no longer unmutes tiles on the next pool sync.

## Performance

- Both `buildView()` surfaces stay mounted. Mix does not reopen decoders.
- Android two-pane decoder budget stays 2.

## Developer Changes

- `app/pubspec_tv.yaml` is `0.0.3+24`. Sync the TV package-info stub with
  `scripts/aika_stream_version.sh --sync-stub`.
- Design: `docs/superpowers/specs/2026-09-24-epgshare01-country-shards-design.md`
  and `docs/superpowers/specs/2026-09-24-multiview-dj-split-design.md`.

## Breaking Changes

No breaking changes.

## Known Limitations

- No recording.
- No cloud favorites sync.
- No cloud playlists.
- Triple and quad mosaics have no split handle.
- Cast protocol is unchanged.

## Installation

### APK

Sideload uses the upload key. Uninstall a sideload before installing from
Play — Play App Signing will not upgrade a sideload in place.

### Play Store

Drop `Aika-Stream-0.0.3-24.aab` on Internal testing, then promote.
Testers on Play 23 upgrade in place. Never reuse versionCode 23.

## Legal Notice

This application is an IPTV player.

It does not provide television channels, media content, or playlists.

Users are responsible for supplying legally authorized IPTV playlists.

## Checksums

SHA256

```text
Aika-Stream-0.0.3-23.aab 970fee096863000327bc0ef98d8b5eeb66137bd6e1c03699ac961d936c45fb65
Aika-Stream-0.0.3-24.aab c36054851bf95a60e5d188feaad5de2ec237d384dffa848f96b431ad66a03911
```

## Contributors

Generated by GitHub.

## Full Changelog

https://github.com/DevelopersCoffee/airo/compare/aika-stream-v0.0.2...aika-stream-v0.0.3
