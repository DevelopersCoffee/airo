name: m3u_parser
description: M3U/M3U8 playlist parser for Flutter — Rust-accelerated on native platforms with an identical pure-Dart fallback (including web).
version: 1.0.0
homepage: https://developerscoffee.com
repository: https://github.com/DevelopersCoffee/airo_m3u_parser
issue_tracker: https://github.com/DevelopersCoffee/airo_m3u_parser/issues
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=3.18.0"

dependencies:
  airo_m3u_parser: ^1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
