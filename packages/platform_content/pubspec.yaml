name: platform_content
description: Component for platform_content
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  platform_content_types:
    path: ../platform_content_types
  platform_ast:
    path: ../platform_ast

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
