name: platform_plugins
description: Plugin lifecycle and registry for AIRO extensions.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_manifest:
    path: ../platform_manifest
  platform_registry:
    path: ../platform_registry

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.3
