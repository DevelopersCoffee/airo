## 0.0.1

- Replace generated Flutter package boilerplate with typed playlist export
  contract models.
- Add regression tests for filename generation, metadata serialization, and
  export result envelopes.
- Document the package purpose and package-local validation commands.
