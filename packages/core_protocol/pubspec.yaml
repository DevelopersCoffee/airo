name: core_protocol
description: Device-to-device network protocol, Protobuf edge envelopes, discovery schemas, zero-config handshakes, and E2EE payload encryption.
version: 1.1.0
homepage: https://developerscoffee.com
repository: https://github.com/DevelopersCoffee/airo_protocol
issue_tracker: https://github.com/DevelopersCoffee/airo_protocol/issues

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  airo_protocol: ^1.0.0
  crypto: ^3.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
