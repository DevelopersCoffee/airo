name: platform_parser
description: Component for platform_parser
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  platform_ast:
    path: ../platform_ast
  platform_identity:
    path: ../platform_identity
  platform_provider:
    path: ../platform_provider
  flutter:
    sdk: flutter
  platform_pipeline:
    path: ../../packages/platform_pipeline
  platform_content:
    path: ../platform_content

dev_dependencies:
  platform_ast:
    path: ../platform_ast
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
