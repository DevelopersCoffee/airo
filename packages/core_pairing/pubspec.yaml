name: core_pairing
description: Monorepo shim re-exporting airo_pairing.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  airo_pairing:
    git:
      url: https://github.com/DevelopersCoffee/airo_pairing.git
      ref: main
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
