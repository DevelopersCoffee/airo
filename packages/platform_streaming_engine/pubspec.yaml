name: platform_streaming_engine
description: "Receiver-side zero-copy streaming engine bridge (Media3/Kotlin on Android TV and Fire TV)."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  platform_player:
    path: ../platform_player

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
