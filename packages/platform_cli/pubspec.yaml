name: platform_cli
description: Thin CLI wrapper for platform build operations.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_build:
    path: ../platform_build

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
