name: platform_import
description: Component for platform_import
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  platform_pipeline:
    path: ../platform_pipeline
  platform_provider:
    path: ../platform_provider
  platform_identity:
    path: ../platform_identity
  flutter:
    sdk: flutter

dev_dependencies:
  platform_pipeline:
    path: ../platform_pipeline
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
