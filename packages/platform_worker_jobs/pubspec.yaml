name: platform_worker_jobs
description: Cooperative CPU resource scheduler, worker isolate executor, and background task manager.
version: 1.0.0
homepage: https://developerscoffee.com
repository: https://github.com/DevelopersCoffee/airo_job_scheduler
issue_tracker: https://github.com/DevelopersCoffee/airo_job_scheduler/issues

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  airo_job_scheduler: ^1.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
