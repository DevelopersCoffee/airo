name: platform_mcp
description: MCP protocol adapter for the AIRO Tool platform.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_contracts:
    path: ../platform_contracts
  platform_schemas:
    path: ../platform_schemas
  platform_tools:
    path: ../platform_tools
  platform_protocols:
    path: ../platform_protocols

  mocktail: ^1.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
