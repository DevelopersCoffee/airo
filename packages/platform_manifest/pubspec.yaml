name: platform_manifest
description: Declarative schema for AIRO extensions and components.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_contracts:
    path: ../platform_contracts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
