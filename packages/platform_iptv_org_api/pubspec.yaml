name: platform_iptv_org_api
description: Typed, cached, relational client for the public iptv-org API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  core_workers:
    path: ../core_workers

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
