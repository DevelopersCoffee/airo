name: core_orchestration_storage
description: Backend orchestration storage interfaces for Airo V2.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  core_commands:
    path: ../core_commands
  core_device_identity:
    path: ../core_device_identity
  core_presence:
    path: ../core_presence
  core_sessions:
    path: ../core_sessions
  core_watch_progress:
    path: ../core_watch_progress
  equatable: ^2.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  core_pairing:
    path: ../core_pairing
  core_protocol:
    path: ../core_protocol
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
