// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

import '../frb_generated.dart';
import 'm3u.dart';

// These functions are ignored because they are not marked as `pub`: `build_cache_and_index`, `cache_matches`, `channel_search_text`, `cleanup_temporary_artifacts`, `create_index`, `database_error`, `decode_list`, `descriptor_for`, `elapsed_micros`, `encode_list`, `filter_sql`, `finalize_index`, `for_each_channel`, `index_matches`, `insert_channel`, `invalid_argument`, `invalid_cache`, `io_error`, `levenshtein`, `normalize_filters`, `normalize_identity`, `normalize_limit`, `normalize_list`, `normalize_optional`, `normalize_search`, `open`, `page_from_channels`, `read_cache_channel`, `read_fuzzy_page`, `read_optional_string`, `read_page`, `read_ranked_page`, `read_required_string`, `read_string_body`, `read_string_list`, `read_u128`, `read_u32`, `read_u64`, `rebuild_index_from_cache`, `remove_if_exists`, `replace_atomically`, `row_to_channel`, `sibling`, `source_fingerprint`, `to_sql_i64`, `validate_directory_path`, `validate_file_path`, `write_cache_channel`, `write_cache_header`, `write_optional_string`, `write_required_string`, `write_string_list`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `MappedPlaylistCache`, `SourceFingerprint`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `assert_fields_are_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

/// Open a versioned local playlist index and return its first page.
///
/// Existing #814 parser APIs remain unchanged. This API is path-based so raw
/// playlist content is never copied across FRB or the Dart main isolate.
Future<PlaylistIndexOpenResult> openPlaylistIndex({
  required String sourcePath,
  required String cacheDirectory,
  required int firstPageLimit,
}) => RustLib.instance.api.crateApiPlaylistEngineOpenPlaylistIndex(
  sourcePath: sourcePath,
  cacheDirectory: cacheDirectory,
  firstPageLimit: firstPageLimit,
);

/// Read a stable zero-based page without materializing the full playlist.
Future<M3uChannelPage> pagePlaylistIndex({
  required String indexPath,
  required int offset,
  required int limit,
}) => RustLib.instance.api.crateApiPlaylistEnginePagePlaylistIndex(
  indexPath: indexPath,
  offset: offset,
  limit: limit,
);

/// Search indexed channel fields and return a bounded stable page.
Future<M3uChannelPage> searchPlaylistIndex({
  required String indexPath,
  required String query,
  required int offset,
  required int limit,
}) => RustLib.instance.api.crateApiPlaylistEngineSearchPlaylistIndex(
  indexPath: indexPath,
  query: query,
  offset: offset,
  limit: limit,
);

/// Search text and/or structured fields through one deterministic native API.
///
/// Text ranking is exact title, title prefix, alias exact/prefix, field
/// substring, then bounded trigram fuzzy matching. Structured filters use AND
/// semantics and may be supplied without text for IntentCommand execution.
Future<M3uChannelPage> searchPlaylistIndexV2({
  required String indexPath,
  required List<PlaylistSearchFilter> filters,
  required int offset,
  required int limit,
  String? query,
}) => RustLib.instance.api.crateApiPlaylistEngineSearchPlaylistIndexV2(
  indexPath: indexPath,
  query: query,
  filters: filters,
  offset: offset,
  limit: limit,
);

class M3uChannelPage {
  const M3uChannelPage({
    required this.channels,
    required this.offset,
    required this.total,
    required this.hasMore,
  });
  final List<M3uChannel> channels;
  final int offset;
  final int total;
  final bool hasMore;

  @override
  int get hashCode =>
      channels.hashCode ^ offset.hashCode ^ total.hashCode ^ hasMore.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is M3uChannelPage &&
          runtimeType == other.runtimeType &&
          channels == other.channels &&
          offset == other.offset &&
          total == other.total &&
          hasMore == other.hasMore;
}

enum PlaylistCacheStatus { coldBuilt, warmOpened, indexRebuiltFromCache }

class PlaylistEngineError implements FrbException {
  const PlaylistEngineError({required this.code, required this.message});
  final PlaylistEngineErrorCode code;
  final String message;

  @override
  int get hashCode => code.hashCode ^ message.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PlaylistEngineError &&
          runtimeType == other.runtimeType &&
          code == other.code &&
          message == other.message;
}

enum PlaylistEngineErrorCode { invalidArgument, io, invalidCache, database }

class PlaylistIndexDescriptor {
  const PlaylistIndexDescriptor({
    required this.indexPath,
    required this.cachePath,
    required this.totalChannels,
    required this.sourceSizeBytes,
    required this.sourceModifiedNanos,
  });
  final String indexPath;
  final String cachePath;
  final int totalChannels;
  final BigInt sourceSizeBytes;
  final BigInt sourceModifiedNanos;

  @override
  int get hashCode =>
      indexPath.hashCode ^
      cachePath.hashCode ^
      totalChannels.hashCode ^
      sourceSizeBytes.hashCode ^
      sourceModifiedNanos.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PlaylistIndexDescriptor &&
          runtimeType == other.runtimeType &&
          indexPath == other.indexPath &&
          cachePath == other.cachePath &&
          totalChannels == other.totalChannels &&
          sourceSizeBytes == other.sourceSizeBytes &&
          sourceModifiedNanos == other.sourceModifiedNanos;
}

class PlaylistIndexOpenResult {
  const PlaylistIndexOpenResult({
    required this.descriptor,
    required this.firstPage,
    required this.parseStats,
    required this.cacheStatus,
    required this.timings,
  });
  final PlaylistIndexDescriptor descriptor;
  final M3uChannelPage firstPage;
  final M3uParseStats parseStats;
  final PlaylistCacheStatus cacheStatus;
  final PlaylistOpenTimings timings;

  @override
  int get hashCode =>
      descriptor.hashCode ^
      firstPage.hashCode ^
      parseStats.hashCode ^
      cacheStatus.hashCode ^
      timings.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PlaylistIndexOpenResult &&
          runtimeType == other.runtimeType &&
          descriptor == other.descriptor &&
          firstPage == other.firstPage &&
          parseStats == other.parseStats &&
          cacheStatus == other.cacheStatus &&
          timings == other.timings;
}

class PlaylistOpenTimings {
  const PlaylistOpenTimings({
    required this.totalMicros,
    required this.sourceMapMicros,
    required this.indexBuildMicros,
    required this.firstPageMicros,
  });
  final BigInt totalMicros;
  final BigInt sourceMapMicros;
  final BigInt indexBuildMicros;
  final BigInt firstPageMicros;

  @override
  int get hashCode =>
      totalMicros.hashCode ^
      sourceMapMicros.hashCode ^
      indexBuildMicros.hashCode ^
      firstPageMicros.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PlaylistOpenTimings &&
          runtimeType == other.runtimeType &&
          totalMicros == other.totalMicros &&
          sourceMapMicros == other.sourceMapMicros &&
          indexBuildMicros == other.indexBuildMicros &&
          firstPageMicros == other.firstPageMicros;
}

enum PlaylistSearchField {
  title,
  alias,
  genre,
  language,
  country,
  provider,
  tag,
}

class PlaylistSearchFilter {
  const PlaylistSearchFilter({
    required this.field,
    required this.operator_,
    required this.value,
  });
  final PlaylistSearchField field;
  final PlaylistSearchOperator operator_;
  final String value;

  @override
  int get hashCode => field.hashCode ^ operator_.hashCode ^ value.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PlaylistSearchFilter &&
          runtimeType == other.runtimeType &&
          field == other.field &&
          operator_ == other.operator_ &&
          value == other.value;
}

enum PlaylistSearchOperator { equals, prefix, contains }
