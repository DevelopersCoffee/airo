name: platform_composition
description: Platform composition engine, feature loading, and service location.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_contracts:
    path: ../platform_contracts
  platform_registry:
    path: ../platform_registry
  platform_registry:
    path: ../platform_registry
  platform_services:
    path: ../platform_services

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.3
