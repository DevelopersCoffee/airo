name: core_domain
description: "Core domain layer: entities, repository interfaces, and use cases"
version: 0.0.1

environment:
  sdk: ^3.9.2

dependencies:
  equatable: ^2.0.7
  meta: ^1.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.0

