name: core_domain
description: "Core domain layer: entities, repository interfaces, and use cases"
version: 0.0.1

environment:
  sdk: ">=3.12.2 <4.0.0"

dependencies:
  equatable: ^2.1.0
  meta: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
  mocktail: ^1.0.5
