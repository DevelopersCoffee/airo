name: platform_registry
description: Universal extension registry, capability indexing, and dependency resolution.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_contracts:
    path: ../platform_contracts
  platform_registry:
    path: ../platform_registry

  mocktail: ^1.0.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
