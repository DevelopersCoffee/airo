name: platform_engine_testkit
description: Executable compliance suite for AIRO inference engines.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  platform_engine_sdk:
    path: ../platform_engine_sdk
  platform_validation:
    path: ../platform_validation

dev_dependencies:
  flutter_lints: ^4.0.0
  mocktail: ^1.0.3
