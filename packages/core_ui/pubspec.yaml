name: core_ui
description: "Core UI package: theme, design tokens, and shared widgets"
version: 0.0.1

environment:
  sdk: ">=3.9.2 <4.0.0"
  flutter: ">=3.35.7"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
  alchemist: ^0.13.0  # Golden testing (replaces discontinued golden_toolkit)

flutter:
  uses-material-design: true

