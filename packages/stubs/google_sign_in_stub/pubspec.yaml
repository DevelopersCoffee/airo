name: google_sign_in
description: Local no-op Google Sign-In stub for simulator and CI builds.
publish_to: none
version: 7.2.0
publish_to: 'none'

environment:
  sdk: ">=3.12.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
