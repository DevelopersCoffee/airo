name: engine_litert
description: Component for engine_litert
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_runtime:
    path: ../platform_runtime
  platform_embeddings:
    path: ../platform_embeddings
  platform_identity:
    path: ../platform_identity

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
