name: core_sessions
description: Playback session sync and handoff contracts for Airo devices.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  core_commands:
    path: ../core_commands
  core_media_routing:
    path: ../core_media_routing
  core_pairing:
    path: ../core_pairing
  core_protocol:
    path: ../core_protocol
  equatable: ^2.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
