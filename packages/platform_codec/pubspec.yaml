name: platform_codec
description: Component for platform_codec
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  platform_provider:
    path: ../platform_provider
  platform_identity:
    path: ../platform_identity
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
