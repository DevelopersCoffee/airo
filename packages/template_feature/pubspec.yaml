name: template_feature
description: "Template package for creating new features. Copy this as a starting point."
version: 0.0.1

environment:
  sdk: ^3.9.2
  flutter: ">=3.35.7"

dependencies:
  flutter:
    sdk: flutter
  core_domain:
    path: ../core_domain
  core_data:
    path: ../core_data
  core_ui:
    path: ../core_ui
  flutter_riverpod: ^3.3.2
  riverpod: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
