name: platform_transform
description: Component for platform_transform
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  platform_content:
    path: ../platform_content

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
