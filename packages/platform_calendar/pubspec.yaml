name: platform_calendar
description: Monorepo shim re-exporting airo_calendar.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=3.0.0"

dependencies:
  airo_calendar:
    git:
      url: https://github.com/DevelopersCoffee/airo_calendar.git
      ref: main
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
