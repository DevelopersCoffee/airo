# platform_text_embeddings

Framework-owned, release-selectable local text embedding for Airo. Android uses
a telemetry-free MediaPipe Text Embedder runtime. The package never downloads,
persists, logs, or deletes caller-owned model files.

The converted model is not bundled in this package or any app profile.
